# Production Environment Configuration
# This file is used when building for production (bun run build)

# API Configuration
# Update this to your production API URL
VITE_API_BASE_URL=https://api.boostpay.com

# Authentication Configuration
# IMPORTANT: Set to 'false' in production to disable auto-login
# Users must login through the proper authentication flow
VITE_USE_DEV_TOKEN=false
